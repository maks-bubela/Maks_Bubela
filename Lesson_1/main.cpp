#include <iostream>

using namespace std;

int main()
{
    int a,b,sum;
    cout <<"Insert the number : ";
    cin >> a;
    cout <<"Insert the number : ";
    cin >> b;
    sum = a+b;
    cout <<"The result is : " << sum;
}
